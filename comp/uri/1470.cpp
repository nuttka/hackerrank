#include <bits/stdc++.h>
#define _                        \
   ios_base::sync_with_stdio(0); \
   cin.tie(0);
#define endl '\n'
#define MAX 123

using namespace std;



int main() {
    _;

    int n, m, in[MAX], out[MAX];
    while (cin >> n) {
        for (int i=0; i<n; i++) {
            cin >> in[i];
        }
        cin >> m;
        for (int i=0; i<m; i++) {
            cin >> out[i];
        }
    


        
    
    }
    return 0;
}